<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.moyada.sharingan.storage.mysql.dao.HttpDAO">

	<resultMap id="httpDO" type="cn.moyada.sharingan.storage.api.domain.HttpDO">
		<id column="id" property="id"/>
		<result column="app_id" property="appId"/>
		<result column="service_id" property="serviceId"/>
		<result column="method_name" property="methodName"/>
		<result column="method_type" property="methodType"/>
		<result column="param" property="param"/>
		<result column="header" property="header"/>
		<result column="expression" property="expression"/>
	</resultMap>

	<sql id="baseColumn">
		`id`, `app_id`, `service_id`, `method_name`, `method_type`, `param`, `header`, `expression`
	</sql>

	<insert id="save">
		INSERT INTO `http_info`
		(<include refid="baseColumn"/>)
		VALUE
		(NULL, #{appId}, #{serviceId}, #{methodName}, #{methodType}, #{param}, #{header}, #{expression});
	</insert>

	<select id="findByService" resultMap="httpDO">
		SELECT <include refid="baseColumn"/>
		FROM `http_info`
		WHERE service_id = #{serviceId};
	</select>

	<select id="findById" resultMap="httpDO">
		SELECT <include refid="baseColumn"/>
		FROM `http_info`
		WHERE id = #{methodId} LIMIT 1;
	</select>
</mapper>