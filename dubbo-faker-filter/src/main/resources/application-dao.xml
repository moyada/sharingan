<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://www.springframework.org/schema/beans"
    xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd"
       default-lazy-init="true">

    <!-- 配置sqlSessionFactory -->
    <bean id="sqlSessionFactory" name="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
        <!-- 连接池对象 -->
        <property name="dataSource" ref="dataSource" />
        <!-- 扫描mapper需要xml文件 -->
        <property name="mapperLocations" value="classpath*:sqlmaps/faker.xml"/>
        <!-- 扫描实体使用别名 -->
        <property name="typeAliasesPackage" value="cn.moyada.dubbo.faker.filter.domain" />
    </bean>

    <!-- 配置扫描DAO接口包，动态实现DAO接口注入 -->
    <bean class="org.mybatis.spring.mapper.MapperScannerConfigurer">
        <property name="sqlSessionFactoryBeanName" value="sqlSessionFactory"/>
        <property name="basePackage" value="cn.moyada.dubbo.faker.filter.dao"/>
    </bean>

    <!-- 配置单个DAO接口实现，注入spring容器 -->
    <bean id="fakerMapper" class="org.mybatis.spring.mapper.MapperFactoryBean">
        <property name="sqlSessionFactory" ref="sqlSessionFactory" />
        <property name="mapperInterface" value="cn.moyada.dubbo.faker.filter.dao.FakerDAO"/>
    </bean>
</beans>
